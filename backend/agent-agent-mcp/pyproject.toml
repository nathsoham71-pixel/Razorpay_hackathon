[project]
name = "agent-agent-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Soham Nath", email = "nathsoham71@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=2.1.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
agent-agent-mcp = "agent_agent_mcp:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
